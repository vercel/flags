---
'@vercel/flags-core': minor
'@flags-sdk/vercel': minor
---

Accept `flags:projectId=<id>` connection strings for reading another project's flags with the deployment's OIDC token. No change for SDK keys or the default OIDC setup.
