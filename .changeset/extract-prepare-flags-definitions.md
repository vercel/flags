---
"@vercel/prepare-flags-definitions": minor
---

Initial release of `@vercel/prepare-flags-definitions`. Extracts the core flag definitions preparation logic from the Vercel CLI into a standalone, reusable package.
